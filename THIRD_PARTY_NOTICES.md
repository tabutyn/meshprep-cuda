# Third-party notices

The library uses CUB headers distributed with the CUDA Toolkit. CUDA and CUB are NVIDIA products and are governed by their own license terms. NVTX header annotations are compiled from the CUDA Toolkit when enabled.

Benchmark scripts can fetch `nolmoonen/cuda-lbvh` at a pinned revision. Its source is not part of this distribution. Benchmark scene archives are not redistributed; their manifest points to upstream and original sources. Users must review those licenses themselves.

No code, binary, Python bytecode, Blender script, or game asset from the separate historical packaging repository is included in a release checkout.

The checker-cylinder GLB, texture, preview, metadata, and converted soft-body
fixture under `assets/softbody/` are original project-generated test assets,
not third-party content. Their generation and ownership are documented in
`docs/PROVENANCE.md`.
